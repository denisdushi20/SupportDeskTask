# Support Desk (Angular)

Angular 20 frontend for the Support Desk assignment.

Setup, proxy, testing, and architecture notes live in the repository root [`README.md`](../../README.md).

```bash
npm install
npx ng serve
```

Dev server: `http://localhost:4200` (proxies `/api` to `http://localhost:5264`).
